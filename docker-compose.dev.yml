services:
  server:
    volumes:
      - ./config/server.dev.yaml:/etc/wacht/server.yaml:ro

  mock:
    build:
      context: .
      args:
        BINARY: wacht-mock
    ports:
      - "9090:9090"
    restart: unless-stopped
