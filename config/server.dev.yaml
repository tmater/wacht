# Local dev probes Docker service names like "mock", so private targets must
# be allowed here. Hosted/managed probes should keep this disabled.
allow_private_targets: true
probes:
  - id: probe-1
    secret: dev-secret-1-a7f3c921
  - id: probe-2
    secret: dev-secret-2-f18d44be
  - id: probe-3
    secret: dev-secret-3-c6a28d51
seed_user:
  email: dev@wacht.local
  password: dev-password-92b3e4f7
checks:
  - id: check-up
    type: http
    target: http://mock:9090/up
  - id: check-down
    type: http
    target: http://mock:9090/down
    webhook: http://mock:9090/webhook
  - id: check-flap
    type: http
    target: http://mock:9090/flap
  - id: check-slow
    type: http
    target: http://mock:9090/slow
