allow_private_targets: true
auth_rate_limit:
  requests: 100
  window: 1m
probes:
  - id: probe-1
    secret: smoke-secret-1
  - id: probe-2
    secret: smoke-secret-2
  - id: probe-3
    secret: smoke-secret-3
seed_user:
  email: smoke@wacht.local
  password: smoke-password
